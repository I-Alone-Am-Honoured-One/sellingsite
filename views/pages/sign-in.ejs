<%- include('../partials/header') %>
<section class="container section narrow">
  <div class="section-header">
    <div>
      <h1>Welcome back</h1>
      <p class="muted">Sign in to manage your listings and orders.</p>
    </div>
  </div>
  <% if (error) { %>
    <div class="error"><%= error %></div>
  <% } %>
  <form class="form-card" method="POST" action="/auth/sign-in">
    <label>
      Email
      <input type="email" name="email" value="<%= email || '' %>" required /> <!-- Preserve email on error -->
    </label>
    <label>
      Password
      <input type="password" name="password" required /> <!-- Password not preserved for security -->
    </label>
    <button class="button primary" type="submit">Sign in</button>
  </form>
  <div class="form-footer">
    <a href="/auth/forgot-password">Forgot your password?</a>
  </div>
</section>
<%- include('../partials/footer') %>
